# -Desenvolvimento-de-API-REST-Escal-vel-com-Node.js-e-MongoDB1

# API de Eventos

## Como rodar

npm install  
npm run dev  

## Variáveis de ambiente

PORT=
MONGO_URI=
JWT_SECRET=

## Endpoints

POST /register  
POST /login  
GET /events  
POST /events  
PUT /events/:id  
DELETE /events/:id  